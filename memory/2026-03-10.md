# 2026-03-10

- 近期 cron 运行情况：
  - `daily-work-summary` 正常产出并归档了 `2026-03-09_工作总结`，链接为 `https://www.feishu.cn/docx/BF8xdw90aolSiAxrW1McMwYLnTg`。
  - `daily-github-sync-memory-topics` 之前失败根因是 Feishu 投递缺少 target（`Delivering to Feishu requires target <chatId|user:openId|chat:chatId>`）；已修复为投递到 `user:ou_fcd0adcb0651f57d66d427cabb85a963`，之后成功执行。
  - `game-rank-6h` 已从纯占位阶段进展到能写入真实 Steam 榜单部分数据：一轮写入 20 条，下一轮写入 47 条。当前主要瓶颈是 Steam API 超时与 Feishu Bitable 单次 `batch_create` 过大导致写入中断；后续建议改为每批 10~20 条串行写入，并缩短备注字段。
  - `tomorrow-plan-reminder-4h` 正常工作，会从最新工作总结中提炼“明天的计划”提醒 Steven。
- recruiter 相关：
  - `recruiter` 已绑定 `feishu:default`，并在其 `SOUL.md` 中加入硬规则：只处理群 `oc_5df1872ab0fe7a8a2ba850f02d4e02cb`，其它群/私聊不处理。
  - 群消息接收问题的重要前提：`channels.feishu.groupPolicy=allowlist` 时必须确保 `groupAllowFrom` 包含 Steven 的 open_id `ou_fcd0adcb0651f57d66d427cabb85a963`，否则群消息会被静默丢弃。
  - 另一个高频根因：机器人/应用若未真正加入群，也不可能收到群消息。
- 身份设定更新：Steven 明确指定 oneone 的形象是一只“可爱的小海豚”，关键词是乐趣、开朗、阳光、可爱、治愈；已更新 `IDENTITY.md`。
