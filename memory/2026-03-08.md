# 2026-03-08

- 进展：尝试将 5 秒测试视频 `/tmp/openclaw/test-5s.mp4` 直接发送到飞书聊天框（“直发”）。
- 关键结论：当前 OpenClaw `message.sendAttachment` 在 Feishu 渠道 **不支持** 直接用 `buffer`（base64）发送；接口报错：`Feishu sendAttachment requires a 'media' or 'url' parameter with the file URL or path.`
- 进一步现象：此前用 `media=/tmp/openclaw/test-5s.mp4` 走本地路径上传返回 400；综合判断 Feishu 插件可能不支持 mp4 作为附件直发，或需要走另一套“先上传文件拿到 file_key / file_token，再发 file 消息”的链路（当前工具层未暴露/未打通）。
- 决策状态：向用户说明“直发目前不可行”，回退到“上传飞书云空间根目录并返回链接”的交付方式。

- 交付：根据用户两张图（舞台胡子哥 + 柔和水下光海豚）制作 4 格漫画竖版视频。
  - 基础版：`four_panel_12s.mp4` 上传到飞书云空间根目录并回传链接（token: `WbeAbm07goSUSQxuhujcicpUn5e`）。
  - 加强版：`four_panel_plus_12s.mp4`（更粗漫画边框、角标 1/4-4/4、对白气泡更明显、行动线、第 4 格上帝视角拉远更强）上传并回传链接（token: `EV71bDRJDo8vuixQxWtctLBPnwg`）。
  - 字幕定稿：1) 又是新的一天。把今天做到极致。 2) 是啊……也许今天就会诞生一个让人快乐的点子。紧张，但更期待。 3) 开始吧。 4) 可爱的人类。

- 安装：按用户要求安装技能仓库 `https://github.com/peterskoett/self-improving-agent`。
  - 克隆到：`/Users/steven/.openclaw/workspace/self-improving-agent`
  - 初始化学习日志目录：`/Users/steven/.openclaw/workspace/.learnings/`（创建了 `LEARNINGS.md`，补齐了 `ERRORS.md`/`FEATURE_REQUESTS.md` 模板文件）
  - 备注：尝试用 `openclaw hooks enable self-improvement` 失败（Hook 名未注册）；改用 `openclaw hooks install -l /Users/steven/.openclaw/workspace/self-improving-agent/hooks/openclaw` 安装 hook pack，提示需重启 gateway。

- 安装：按用户要求安装 `https://github.com/zhaoxinyi02/ClawPanel`。
  - 克隆到：`/Users/steven/.openclaw/workspace/ClawPanel`
  - 安装脚本：`./scripts/install.sh` 在 macOS 上要求 root/sudo；以非 sudo 运行会报：`[ClawPanel] 请使用 root 用户或 sudo 运行此脚本！`
  - 当前状态：已执行 `sudo bash scripts/install.sh`，进程在等待终端输入 sudo 密码（提示 `Password:`），需要用户在本机终端输入后才能继续。
