
## 2026-03-07 晚间补记｜game-rank-6h cron：从 Bitable 改为写 Docx《游戏每日排名》推进记录
- 用户要求：将“定时任务3（game-rank-6h）”的查询结果写入飞书文档，文档名《游戏每日排名》，并新建 doc。
- 现状定位（关键）：
  - feishu_bitable_* 工具为“以用户身份”调用，cron isolated 会话拿不到用户 OAuth 上下文，导致 need_user_authorization。
  - 用户已在飞书完成一次 OAuth 授权；在当前对话中 Bitable list 已成功，证明授权 token 生效；但 cron isolated 仍报 need_user_authorization。
  - 尝试将 cron sessionTarget 改为 main 失败：OpenClaw 限制 main cron 必须 payload.kind=systemEvent（报错：main cron jobs require payload.kind="systemEvent"）。
- 方案2（应用身份/tenant token）落地：
  - 创建脚本：~/.openclaw/scripts/feishu_tenant_bitable_write.js（tenant_access_token/internal + Bitable OpenAPI），首次调用被拒（99991672 缺 bitable scopes），用户开通后 fields/records 读取成功。
  - 仍写入 403（91403 Forbidden）：说明还需给应用授予该 Bitable 文件/表的编辑权限（共享/权限/协作成员加入应用）。
- 按用户要求改为写 Docx《游戏每日排名》：
  - 创建脚本：~/.openclaw/scripts/game_rank_to_doc.js（tenant token 读取两张 Bitable 表，并创建 docx 文档写入内容）。
  - docx create：最初 99991672 缺 docx scopes；用户已通过官方链接开通 docx:document / docx:document:create 后，docx 创建成功（调试：document_id=OM4ed9Gmao1ibVxUgOYcoCWjnod）。
  - docx 写入 blocks 失败：POST /docx/v1/documents/{docId}/blocks/{blockId}/children 返回 1770001 invalid param。blocks list 显示根 block_type=1 page，block_id=docId；但以 block_type=2 paragraph+elements(text_run) 的 payload 仍被判 invalid param，说明需要按飞书 docx block schema 精确构造或改用更稳写入路径。
- 安全/运维措施：为避免反复失败刷屏，曾将 cron#3 disable；后续根据用户新需求暂时 re-enable 以继续改造。
- 当前待决：用户需在“1) 允许走上传/导入 markdown/txt 生成 doc 或 2) 坚持 docx blocks 直写（需研究正确 JSON）”中选择，以尽快跑通《游戏每日排名》自动化。
