
## 19:59 — Discord-like Web 原型（Next.js）立项与实现结果
- 用户明确选择：原型先做 Web（Next.js）。
- 已在 `mvp-prototype` 内新增一套 **Discord-like MVP** 路由与页面骨架，并通过 `npm run build`（Next.js 16.1.6）。
- IA/MVP 设计要点：先保证“Server(=Guild)→Channel(文字/语音)→RBAC(简化)”最小闭环可演示。

### 可演示路由
- 入口：`/discord`
- 服务器列表：`/discord/servers`
- 示例服务器频道列表：`/discord/servers/wg`
- 文字频道聊天（localStorage 持久化）：`/discord/servers/wg/channel/c1`、`/discord/servers/wg/channel/c2`
- 语音频道（占位：加入/离开 + 成员状态）：`/discord/servers/wg/voice/v1`、`/discord/servers/wg/voice/v2`
- 成员列表：`/discord/servers/wg/members`
- 角色与权限（简化 4 权限开关）：`/discord/servers/wg/settings/roles`
- 创建/加入（原型占位，跳转 wg）：`/discord/servers/new`、`/discord/servers/join`

### 关键文件
- Mock 数据与 localStorage key：`mvp-prototype/src/lib/discord-mock.ts`（消息 key：`wg.discord.msg.{serverId}.{channelId}`；语音加入 key：`wg.discord.voice.{serverId}.{channelId}.joined`）
- Discord 原型壳（frosted 顶栏）：`mvp-prototype/src/components/discord/discord-shell.tsx`
- 页面：`mvp-prototype/src/app/discord/**`

### 后续建议（产品化方向）
1) 未读/回流：server 未读点、channel 未读点、跳到最后未读。
2) 权限“真正生效”：发言/管理入口处按 RBAC 拦截（目前仅 UI 开关）。
3) 语音接入：WebRTC + SFU（mediasoup/livekit）替换占位状态。

### 工程注意
- 动态路由路径含 `[id]`、`[channelId]` 的写文件/命令需加引号，避免 zsh glob（之前已踩坑并修复）。
- 本次在 workspace 根目录进行了首次 git commit（包含大量文件与 node_modules 等，后续需要优化 `.gitignore` / 拆分 repo）。
